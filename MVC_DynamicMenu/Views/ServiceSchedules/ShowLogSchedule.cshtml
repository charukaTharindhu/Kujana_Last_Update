
@model List<ServiceLog>
@{
    ViewData["Title"] = "Home page";
}
<head>
    <link href="~/css/Addfile.css" rel="stylesheet" />
</head>
<div class="container">
    <header class="header">
        <div class="text-right">
            <button type="button" onclick='window.location.href = "/ServiceSchedules/AddNewLog"' class="btn btn-primary">+New Entry</button>
        </div>

        <h1 id="title" class="text-center">Show All Service Schedule Log</h1>
        <p id="description" class="description text-center">

        </p>
    </header>

    <form id="survey-form">
        <div class="form-group">
            <label id="name-label" for="name">Select Participant</label>
            <input type="text"
                   name="name"
                   id="name"
                   class="form-control"
                   placeholder="Enter name"
                   required />
        </div>

        @*Date Start*@
        <label for="dateofbirth"></label>
        <input type="date" name="dateofbirth" id="dateofbirth">
        @*Date Start*@
        <label for="dateofbirth"></label>
        <input type="date" name="dateofbirth" id="dateofbirth">

        @*Hierarchy Start*@
        <div class="form-group">
            <p>Please Select a State</p>
            <select id="dropdown" name="role" class="form-control" required>
                @*<option disabled selected value>Select current role</option>*@
                <option value="student"></option>
                <option value="job">Full Time Job</option>
                <option value="learner">Full Time Learner</option>
                <option value="preferNo">Prefer not to say</option>
                <option value="other">Other</option>
            </select>
        </div>

        <div class="row">
            <div class="col-md-12">
                <table class="table badge-light">
                    <thead>
                        <tr>
                            <th>Reference</th>
                            <th>SSID</th>
                            <th>Participant</th>
                            <th>Start Date</th>
                            <th>End Date</th>
                            <th>Service</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.Reference</td>
                            <td>@item.SSID</td>
                            <td>@item.Client_name</td>
                            <td>@item.Start_date</td>
                            <td>@item.End_date</td>
                            <td>@item.Service</td>
                            <td>@item.Status</td>
                            <td>
                                <a asp-controller="ServiceSchedules" asp-action="UpdateSL" asp-route-id="@item.Reference">Edit</a>
                                <a asp-controller="ServiceSchedules" asp-action="DeleteSL" asp-route-id="@item.Reference">Delete</a>
                                <a asp-controller="ServiceSchedules" asp-action="" asp-route-id="@item.Reference">publish</a>
                            </td>
                        </tr>
                    }
                </table>
            </div>
        </div>

        <script type="text/javascript">
            const projectName = 'survey-form';

        </script>
    </form>

</div>



